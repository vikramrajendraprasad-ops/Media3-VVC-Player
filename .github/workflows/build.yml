name: Build Media3 Pro Player (VVC+APV+IAMF)
on: [workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Media3
        uses: actions/checkout@v4
        with:
          repository: androidx/media
          path: media
          ref: main

      - name: Install 2026 Build Tools
        [cite_start]run: sudo apt-get update && sudo apt-get install -y nasm yasm make [cite: 1, 2]

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup NDK r26b
        id: setup-ndk
        uses: nttld/setup-ndk@v1
        with:
          [cite_start]ndk-version: r26b # Fixed indentation here 

      - name: Prepare FFmpeg Source
        run: |
          cd media/libraries/decoder_ffmpeg/src/main/jni [cite: 4]
          git clone --depth 1 https://github.com/FFmpeg/FFmpeg.git ffmpeg [cite: 4]

      - name: Build Native Core (VVC+APV+IAMF+Opus)
        run: |
          cd media/libraries/decoder_ffmpeg/src/main/jni 
          chmod +x build_ffmpeg.sh 
          sed -i 's/ENABLED_ABIS=(.*/ENABLED_ABIS=(arm64-v8a)/g' build_ffmpeg.sh 
          sed -i 's/--disable-postproc//g' build_ffmpeg.sh 
          ./build_ffmpeg.sh "$(pwd)/.." "${{ steps.setup-ndk.outputs.ndk-path }}" "linux-x86_64" "24" vvc apv iamf opus h264 hevc 

      - name: 🛡️ Surgery on Media3 Core (Force Video Decoding)
        run: |
          # Robust search for source files
          PLAYER=$(find media -name "PlayerActivity.java" | grep "demo/main" | head -n 1)
          CHOOSER=$(find media -name "SampleChooserActivity.java" | grep "demo/main" | head -n 1)
          FACTORY=$(find media -name "DefaultRenderersFactory.java" | head -n 1)
          FF_VIDEO=$(find media -name "FfmpegVideoRenderer.java" | head -n 1)
          MIME_UTIL=$(find media -name "MimeTypes.java" | head -n 1)
          LIB_FILE=$(find media -name "FfmpegLibrary.java" | head -n 1)

          # 1. Register VVC/APV MimeTypes in core engine
          sed -i '/public static final String VIDEO_H265 =/a \  public static final String VIDEO_VVC = "video/vvc";\n  public static final String VIDEO_APV = "video/apv";' "$MIME_UTIL"
          
          # 2. Tell FfmpegVideoRenderer to handle these formats
          sed -i '/VIDEO_H264/a \    case MimeTypes.VIDEO_VVC:\n    case MimeTypes.VIDEO_APV:' "$FF_VIDEO"
          
          # 3. Register in native library list 
          sed -i '/"video\/hevc"/a \    "video\/vvc",\n    "video\/apv",\n    "audio\/iamf",' "$LIB_FILE"

          # 4. Force Preference for Extensions 
          sed -i 's/extensionRendererMode = EXTENSION_RENDERER_MODE_OFF/extensionRendererMode = EXTENSION_RENDERER_MODE_PREFER/g' "$FACTORY"

      - name: 🛡️ Inject UI & Fixed Action Bridge
        run: |
          ACTIVITY=$(find media -name "SampleChooserActivity.java" | grep "demo/main" | head -n 1) [cite: 6]
          
          # Inject The Purple Button (UI) [cite: 7, 8]
          sed -i '/import android.widget.Button;/d' "$ACTIVITY"
          sed -i '/setContentView/a \
            android.widget.Button proBtn = new android.widget.Button(this);\
            proBtn.setText("📂 OPEN LOCAL FILE (VVC/APV)");\
            proBtn.setBackgroundColor(0xFF6200EE);\
            proBtn.setTextColor(0xFFFFFFFF);\
            proBtn.setPadding(30, 30, 30, 30);\
            proBtn.setOnClickListener(v -> {\
                android.content.Intent intent = new android.content.Intent(android.content.Intent.ACTION_OPEN_DOCUMENT);\
                intent.addCategory(android.content.Intent.CATEGORY_OPENABLE);\
                intent.setType("*/*");\
                startActivityForResult(intent, 1001);\
            });\
            android.widget.FrameLayout.LayoutParams params = new android.widget.FrameLayout.LayoutParams(-1, 200);\
            params.gravity = android.view.Gravity.TOP;\
            addContentView(proBtn, params);' "$ACTIVITY"

          # Inject Bridge Logic [cite: 9, 10, 11]
          python3 - <<EOF
          import os
          path = '$ACTIVITY'
          if os.path.exists(path):
              with open(path, 'r') as f:
                  content = f.read()
              last_brace = content.rfind('}')
              bridge_logic = '''
              @Override
              public void onActivityResult(int requestCode, int resultCode, android.content.Intent data) {
                  super.onActivityResult(requestCode, resultCode, data);
                  if (requestCode == 1001 && resultCode == RESULT_OK && data != null && data.getData() != null) {
                      android.net.Uri uri = data.getData();
                      android.content.Intent playerIntent = new android.content.Intent("androidx.media3.demo.main.action.VIEW"); [cite: 12]
                      playerIntent.setClass(this, androidx.media3.demo.main.PlayerActivity.class); [cite: 13]
                      playerIntent.setDataAndType(uri, "video/vvc"); # Force video type 
                      playerIntent.addFlags(android.content.Intent.FLAG_GRANT_READ_URI_PERMISSION); 
                      playerIntent.putExtra("prefer_extension_decoders", true); 
                      playerIntent.putExtra("uri", uri.toString()); [cite: 15]
                      playerIntent.putExtra("title", "Local VVC File"); [cite: 16]
                      startActivity(playerIntent); [cite: 16]
                  }
              }
              '''
              [cite_start]new_content = content[:last_brace] + bridge_logic + content[last_brace:] [cite: 17]
              with open(path, 'w') as f:
                  [cite_start]f.write(new_content) [cite: 17]
          EOF

      - name: Final Compilation
        run: |
          cd media
          chmod +x gradlew
          find . -name "AndroidManifest.xml" -exec sed -i 's/package="androidx.media3[^"]*"//g' {} + [cite: 19]
          ./gradlew :demo:assembleDebug [cite: 19]
          mkdir -p ../output_apk
          find . -name "*.apk" -not -path "*/build-cache/*" -exec cp {} ../output_apk/VVC_Pro_Player.apk \; [cite: 19]

      - name: Upload Artifact
        [cite_start]uses: actions/upload-artifact@v4 [cite: 20]
        with:
          [cite_start]name: Media3-Pro-Player [cite: 20]
          [cite_start]path: output_apk/VVC_Pro_Player.apk [cite: 20]
