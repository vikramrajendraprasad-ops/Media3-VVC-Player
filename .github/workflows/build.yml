name: Build Media3 Pro (VVC + APV + IAMF)
on: [workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Media3
        uses: actions/checkout@v4
        with:
          repository: androidx/media
          path: media
          ref: main

      - name: Install Dependencies
        run: sudo apt-get update && sudo apt-get install -y nasm yasm make

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup NDK r26b
        id: setup-ndk
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r26b

      - name: Prepare FFmpeg Source
        run: |
          cd media/libraries/decoder_ffmpeg/src/main/jni
          git clone --depth 1 https://github.com/FFmpeg/FFmpeg.git ffmpeg

      - name: Build Native FFmpeg (Full Codec Support)
        run: |
          cd media/libraries/decoder_ffmpeg/src/main/jni
          chmod +x build_ffmpeg.sh
          sed -i 's/ENABLED_ABIS=(.*/ENABLED_ABIS=(arm64-v8a)/g' build_ffmpeg.sh
          sed -i 's/--disable-postproc//g' build_ffmpeg.sh
          # Building the heavy lifters: vvc, apv, iamf
          ./build_ffmpeg.sh "$(pwd)/.." "${{ steps.setup-ndk.outputs.ndk-path }}" "linux-x86_64" "24" vvc apv iamf opus h264 hevc

      - name: 🛡️ Media3 Core Surgery (No Variable Definitions)
        run: |
          # 1. Locate files
          MIME_UTIL=$(find media -name "MimeTypes.java" | head -n 1)
          FF_AUDIO=$(find media -name "FfmpegAudioRenderer.java" | head -n 1)
          LIB_FILE=$(find media -name "FfmpegLibrary.java" | head -n 1)
          FACTORY=$(find media -name "DefaultRenderersFactory.java" | head -n 1)
          PLAYER=$(find media -name "PlayerActivity.java" | head -n 1)
          CHOOSER=$(find media -name "SampleChooserActivity.java" | head -n 1)
          DEMO_UTIL=$(find media -name "DemoUtil.java" | head -n 1)

          # 2. Add Codecs to Native Bridge (This is usually empty/safe to append)
          # We check for existence first to be 100% sure
          grep -q "video/vvc" "$LIB_FILE" || sed -i '/"video\/hevc"/a \    "video\/vvc",\n    "video\/apv",' "$LIB_FILE"
          grep -q "audio/iamf" "$LIB_FILE" || sed -i '/"audio\/opus"/a \    "audio\/iamf",' "$LIB_FILE"

          # 3. GLOBAL FORCE: Prefer Extensions
          # This replaces "OFF" with "PREFER" everywhere it appears in the factory and demo utils
          # This bypasses the "variable already defined" error because we aren't defining anything!
          sed -i 's/EXTENSION_RENDERER_MODE_OFF/EXTENSION_RENDERER_MODE_PREFER/g' "$FACTORY"
          sed -i 's/EXTENSION_RENDERER_MODE_OFF/EXTENSION_RENDERER_MODE_PREFER/g' "$DEMO_UTIL"

          # 4. Bypass Intent Actions
          sed -i '/unexpected_intent_action/,/return;/d' "$PLAYER"

      - name: 🛡️ Inject Picker UI
        run: |
          ACTIVITY=$(find media -name "SampleChooserActivity.java" | head -n 1)
          sed -i '/import android.widget.Button;/d' "$ACTIVITY"
          sed -i '/setContentView/a \
            android.widget.Button b = new android.widget.Button(this);\
            b.setText("📂 OPEN VVC/APV/IAMF FILE");\
            b.setOnClickListener(v -> {\
                android.content.Intent i = new android.content.Intent(android.content.Intent.ACTION_OPEN_DOCUMENT);\
                i.addCategory(android.content.Intent.CATEGORY_OPENABLE);\
                i.setType("*/*");\
                startActivityForResult(i, 1001);\
            });\
            addContentView(b, new android.view.ViewGroup.LayoutParams(-1, 300));' "$ACTIVITY"

          python3 - <<EOF
          import os
          path = '$ACTIVITY'
          with open(path, 'r') as f: content = f.read()
          bridge = '''
              @Override
              public void onActivityResult(int requestCode, int resultCode, android.content.Intent data) {
                  super.onActivityResult(requestCode, resultCode, data);
                  if (requestCode == 1001 && resultCode == RESULT_OK && data != null) {
                      android.net.Uri uri = data.getData();
                      android.content.Intent pi = new android.content.Intent("androidx.media3.demo.main.action.VIEW");
                      pi.setClass(this, androidx.media3.demo.main.PlayerActivity.class);
                      pi.setData(uri);
                      pi.addFlags(android.content.Intent.FLAG_GRANT_READ_URI_PERMISSION);
                      pi.putExtra("prefer_extension_decoders", true);
                      pi.putExtra("title", "Pro Media");
                      startActivity(pi);
                  }
              }
          '''
          new_content = content[:content.rfind('}')] + bridge + "}"
          with open(path, 'w') as f: f.write(new_content)
          EOF

      - name: Final Compilation
        run: |
          cd media
          chmod +x gradlew
          ./gradlew :demo:assembleDebug
          mkdir -p ../output_apk
          find . -name "*.apk" -not -path "*/build-cache/*" -exec cp {} ../output_apk/Media3_VVC_Final.apk \;

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: VVC-Player
          path: output_apk/Media3_VVC_Final.apk
